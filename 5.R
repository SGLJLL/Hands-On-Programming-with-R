# Create a character vector for the face names of the cards in a royal flush
face_names <- c("ace", "king", "queen", "jack", "ten")

# Create a character vector for the face names and suit of the cards in a royal flush
face_names <- c("ace", "king", "queen", "jack", "ten", "spades")

# Check the type of the face_names vector
class(face_names)  # character

